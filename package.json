{
  "name": "health-history-system",
  "version": "1.0.0",
  "description": "A health history system with backend and frontend",
  "scripts": {
    "prestart": "npm install --prefix backend && npm install --prefix frontend",
    "start": "docker-compose up --build",
    "start:dev": "docker-compose up",
    "build": "docker-compose build",
    "stop": "docker-compose down",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend"
  },
  "author": "",
  "license": "ISC"
}