{
  "name": "health-history-system-backend",
  "version": "1.0.0",
  "description": "Backend for health history system",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "tsc",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "biome lint .",
    "format": "biome format . --write"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongodb": "^4.1.0",
    "pino": "^9.4.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3",
    "@types/express": "^4.17.13",
    "@types/node": "^16.0.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.0.0",
    "typescript": "^4.3.5"
  }
}
